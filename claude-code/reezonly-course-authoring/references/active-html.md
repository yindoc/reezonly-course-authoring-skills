# Active HTML 24

Read current `reezonly://authoring-guide/active-html-v1` first. Runtime schema/resource authority wins. HTML24 is unrestricted HTML/CSS/JavaScript pass-through, not RichText; do not add local sanitizer/allowlist or invented URL restriction.

Choose tab by learning objective, then read back authoritatively after create/update. Define visual tokens before first block. Each block has a self-contained semantic root, scoped CSS, duplicated base CSS, responsive composition, semantic accessibility and multi-instance-safe JS only when needed. Unknown mutation is never resent.
